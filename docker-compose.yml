services:
  odolog:
    build:
      context: .
      target: dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0
      - PORT=8080
    command: bun run dev --host 0.0.0.0 --port 8080
